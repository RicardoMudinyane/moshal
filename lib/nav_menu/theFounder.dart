import 'package:flutter/material.dart';

class TheFounder extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return new Scaffold(
      appBar: AppBar(title: Text("Founder"),),
      body: Center(),
    );
  }


}